// vitest.config.js (INI FILE BARU YANG HARUS DIBUAT)

import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    // Konfigurasi untuk Vitest ada di sini.
    // Saat ini kita biarkan kosong.
  },
});